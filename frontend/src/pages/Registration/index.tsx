import {
  PageContainer,
  RegistrationForm,
  RegistrationHeader,
} from '@components';

export function Registration() {
  return (
    <PageContainer>
      <RegistrationHeader />
      <RegistrationForm />
    </PageContainer>
  );
}
